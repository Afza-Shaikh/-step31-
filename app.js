"use strict";
//No Users: Add an if test to Exsercise 30 to make sure the list of users is not empty 
//. If the list is empty, print the message we need tp find some uders!
//. Remove all of the usernames from your array, and make sure the correct message is printed
let users = [];
if (users.length > 0) {
    console.log("we have enough users");
}
else {
    console.log("we need to find some user");
}
